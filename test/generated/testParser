#!/bin/sh

TESTBINARY=../../build/bin/testParser
for file in `ls *.txt`
do 
  ERROR=
  ERROR=`${TESTBINARY} $file | grep -i failed`

  if [ "$ERROR" != "" ]; then 
	echo "$file \t\t [ERROR]"
  else 
	echo "$file \t\t [OK]"
  fi
done
